## tech used in project
NodeJS
Express
Postgres


## init project

```bash
cp .env.example to .env
```
```bash

##  Run migration
```bash
npm run migration
```
## Seeder data

1. Run Seeder: `npm run seeder`
2. seeder province data : `npm run seeder:csv`

## Steps to run this project for development:

1. Run `npm i` command
2. Run `npm start` or `npm run start:dev` command